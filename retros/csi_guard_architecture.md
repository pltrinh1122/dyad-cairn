# Retro: Validating the CSI Arm/Disarm Architecture

## Continue
We must continue anchoring all philosophical friction directly into physical mechanics. The design and implementation of both the Symmetric Topology Mass Monitor and the Asymmetric Lock/Linter mechanism proves that pushing rules down to the physical substrate eliminates Generative memory anxiety. The engine acts as its own enforcement vector.

## Start
We must start explicitly mapping our CSI Guards as we build them. Because we adopted a "lean" architecture where locks and closure scripts are highly modular and decoupled, we must ensure new traps and keys are structurally visible. We should maintain a lightweight registry (e.g., in `kb/`) of existing asymmetric locks and their corresponding closure scripts so the Operator always knows exactly how to disarm a newly encountered mechanism without grepping bash logic.

## Stop
We must violently stop relying on `GEMINI.md` for Generative formatting rules (G-enforcement). The explicit failure mode observed today—where the Agent completely hallucinated the definition of "CSS format" because it wasn't physically linted—proves that G-enforcement degrades. No new structural formats or process constraints will be established without an accompanying deterministic linter.
