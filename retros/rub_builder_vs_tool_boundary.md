# Retro: The Builder vs Tool Boundary (The Boundary Lock)

## Continue
We must continue attacking boundary bleeding. The Operator's rub successfully identified a lethal conflation: I assumed `dyad-cairn`'s internal Ledger would serve as the structural "Why" for `dip.py`.

## Start
We must start physically enforcing the **Boundary Lock**. `dyad-cairn` is the Builder (The Mason). `dip.py` is the Tool (The Stone). 
Tools built for the Commons must carry their own isolated Ontological Bond. The TDD tests (The How) and the structural documentation (The Why) for `dip.py` must be packaged *with* the tool, entirely decoupled from `dyad-cairn`'s internal `DYAD_LEDGER.md`. 

## Stop
We must stop using `dyad-cairn`'s internal Retros as the documentation for external tools. `dyad-cairn`'s ledger records *why Cairn made a move*; it does not serve as the spec for the artifact it yields. Conflating the two binds the Tool to the Builder, making it impossible for the Commons to inherit it cleanly.
