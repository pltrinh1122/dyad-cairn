# Retro: Falsifying the Backlog (Residual Shadow)

## Continue
We must continue to aggressively prune obsolete architectural appendages ("dead mass"). The Operator's mandate to identify and eliminate "residual shadows" from our parent dyad (`dyad-wu-wei`) is essential to maintaining a lean, high-velocity Engine.

## Start
We must start treating the local DAG (`artifacts/frontier_state.yml`) as the exclusive source of truth for planning. The PR itself (Red Phase) is the singular structural Intent Gate.

## Stop
We must stop relying on external GitHub Issues for node planning. The `bin/backlog` script, and the global `user_global` instruction mandating its use, are recognized as **Residual Shadows** (obsolete ghosts) from the `dyad-wu-wei` architecture. We are formally deleting `bin/backlog` and excising the redundant remote-issue coupling from the SPAOR loop. The Autonomous Merge Invariant renders tracking issues obsolete.
