# Retro: <Title>

<!-- CSS+OR form, adopted from dyad-bond (github.com/pltrinh1122/dyad-bond,
     kb/reflection-discipline.md). Each line is provenance-tagged: `(OR)` — Operator-Reflect —
     when credit belongs to the Operator's own conduct, not the Agent's; untagged = Agent-observed.
     OR is a tag on CONTINUE, not a fourth section. Per dyad-bond's own falsification record, OR has
     only ever survived in the credit direction (naming a positive Operator pattern) — using it to
     flag Operator drift/incoherence is UNPROVEN there, so treat that direction as candidate, not
     settled, here too. -->

## Continue
<What behaviors/invariants are working and must be maintained? Tag `(OR)` where the credit is the
Operator's own conduct, not the Agent's.>

## Start
<What new mechanical guardrails or behaviors must we adopt based on this retro? (Agent, from live
feedback.)>

## Stop
<What generative or failure-prone behaviors must cease immediately? (Agent, from live feedback.)>
