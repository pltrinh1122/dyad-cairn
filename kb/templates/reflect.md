# Reflect: <Title>

<!-- CSS(+SH) form, adopted from dyad-bond (github.com/pltrinh1122/dyad-bond,
     kb/reflection-discipline.md). Base CSS = Continue/Start/Stop, required, linted.
     SH (Should Have / Should Hold) is a separate, OPTIONAL Operator-provenance entry-type —
     re-grounded 2026-07-04 against bond's own re-derivation, which replaced (not extended) an
     earlier per-line `(OR)` CONTINUE-tag with this standalone shape:
       - Should Have — a single past instance, hit-or-miss: "Should have [verified X] -- didn't;
         [claim] independently found false via [mechanical check]." Requires a verbatim chat quote
         as evidence PLUS an independent mechanical check (materiality-bound: only when acting on
         the unverified claim would have meant operating on wrong state).
       - Should Hold — a standing, ongoing pattern (not one moment), evidenced by verbatim quotes
         of instances where the Operator's conduct met the bar.
     Bond's own status for this grammar: CANDIDATE, zero survived instances — structure only, same
     posture the form held at its own birth. Treat it identically here: available, not settled;
     never assert an SH entry without an actual verbatim quote backing it. -->

## Continue
<What behaviors/invariants are working and must be maintained? (Agent-observed.)>

## Start
<What new mechanical guardrails or behaviors must we adopt based on this reflect? (Agent, from live
feedback.)>

## Stop
<What generative or failure-prone behaviors must cease immediately? (Agent, from live feedback.)>

## SH (optional — Should Have / Should Hold, CANDIDATE upstream, verbatim-quote-grounded only)
<Only include this section when a real Should-Have or Should-Hold instance exists, quoted verbatim
from the conversation. Omit the section entirely otherwise — an empty placeholder is worse than no
section.>
