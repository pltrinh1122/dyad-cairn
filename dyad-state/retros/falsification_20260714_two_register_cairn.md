# Falsification Record: the Two-Register Form Applied to Cairn's Own README (2026-07-14)

**Hypothesis (Operator directive):** dyad-bond's two-register form (`README.md` plain
lens + `FALSIFICATION.md` formal surface) can improve accessibility and readability of
cairn's own `README.md`.

**Method:** measure both of cairn's registers — the live plain lens and the formal
register at `./bin/git show c252640:README.md` (the C13 archaeology object) — against
C24's own trigger condition ("the formal layer's *necessary* mass exceeds what the
declared audience can carry") and against the defects the split would actually cure.

## Refuted (the half that does not transfer)

**"Split the README to make it lighter/more readable."** Bond split an overweight
document (394 lines / 31.3 KB). Cairn's plain README is 93 lines / 8,101 B and already
*is* the plain lens — the 2026-07-08 layman re-projection amputated the formal mass
rather than relocating it. Measured armament in the live README: five one-line knives,
all C10-necessary. There is no residue and no armament to move; a "split for
readability" pass on this document would have ~0% yield (the C21 lesson — a concision
claim with no residue is cutting something else). The README's own readability gain
from this arc is two pointer sentences.

## Survived (the half that transfers, inverted)

**Materialize the missing sibling.** Cairn's formal register existed but was
*archaeological*: reachable only via `./bin/git show c252640` (a command documented
inside HOW-0006, not in the README), never machine-gated, and permanently frozen — the
C12 extension baseline aging in a pinned commit while the plain register keeps
evolving. Three lived defects, one form fixes them: a live `FALSIFICATION.md` gives
the attacker a purpose-built surface (~3.6 KB of formal-only apparatus: 2,477 B of
footnoted extensions + 1,158 B of self-run protocols, previously invisible), becomes
the *living* extension baseline for the Grounding Audit, and both siblings ride the
mechanical gate (`readme_lint` + `--claim-parity`) in CI forever.

**Resurrection required cleaning, which validates C8/C9:** the c252640 formal register
carries bond's inherited metaphor "hands you the knife" (×2), a "house of cards"
domain stray, and "engines"; all removed in the re-projection. The falsifier
*extensions* were carried exactly (C12): the five formal `Break it:` lines in
`FALSIFICATION.md` restate `[^c1]–[^c5]`'s falsifiers with register-only changes,
audited claim-by-claim against the footnote baseline and the four `DYAD.md` canonical
homes.

## Gates run (the discipline exercising itself — first cairn instances)

| Condition | Result |
|---|---|
| C22 knife-freeze | README knives byte-stable through the pass (`--knife-diff` baseline vs edited: empty) |
| C24 claim parity | 6 = 6 across siblings (`--claim-parity`: pass); mutual pointers present |
| C17 lint | both documents pass `readme_lint` |
| C23 post-pass audit | extensions checked against `[^c1]–[^c6]` + canonical homes; no widening/narrowing found |
| C15 honesty fields | `status/grade/coverage/dogma` byte-identical across siblings and to baseline |

## Disposition

`FALSIFICATION.md` created (formal surface, full C2 schema, four-home
`canonical_home`); `README.md` gains the register declaration + mutual pointer and an
`updated` bump only. HOW-0006 updated in consensus: C13's formal-register home is now
the live sibling; the Grounding Audit's extension baseline is
`HEAD:FALSIFICATION.md` where a sibling exists; C24's origin gains the cairn instance
(n=2), C22/C23 likewise; C21 stays n=1. CI now gates both siblings and their parity
(`tests/test_readme_lint.py`). The sibling form's longevity remains open — neither
dyad has lived a third register or a claim-set change across siblings.
