# Coherence Validation: README.md ↔ FALSIFICATION.md (2026-07-14, final pass)

Closing audit of the two-register arc, Operator-directed. Two layers.

## Mechanical (all pass)

- **Frontmatter:** all 12 shared consensus fields byte-equal across siblings —
  `kind, genre, rule, belief{...}, grade, coverage, dogma, caution, cta,
  canonical_home, governed_by, updated`. Only `doc` and the schema comments differ
  (each document names itself and declares its register — intended).
- **Claim parity:** 6 = 6 (`--claim-parity`). **Knives:** 5 = 5 (Claim 6 is the
  standing invitation in both). **Lint:** both conform, C25 links included.
- **Mutual pointers:** README closing → `FALSIFICATION.md`; FALSIFICATION opening ¶
  and closing ¶ → `README.md`.

## Semantic crosswalk (claim-by-claim extension agreement, C12/C24)

| # | Plain knife ↔ formal extension | Verdict |
|---|---|---|
| 1 | "fully-automated agent whose memory… no human checking… keeps working over time" ↔ "autonomous (pure 100% agent) generative memory system that scales across time without structural collapse" | same refutation-set |
| 2 | "halts and asks its human partner… instead of inferring" ↔ "inherently halts and demands reality from the Operator… rather than generating plausible mortar" | same set; Operator-routing preserved (the historically drifted qualifier) |
| 3 | "reliably picks out what matters on its own… as the work runs long" ↔ "autonomously act as The Diviner… without human steering or loss of relevance" | same set |
| 4 | "intent lives entirely inside the code and still holds up" ↔ "philosophical intent durably embedded within execution code without a separate declarative Why" | same set; three-axis bond stated in both bodies |
| 5 | "stays within its own boundaries using nothing but self-control" ↔ "maintains strict extraction boundaries purely via self-policing" | same set |
| 6 | invitation + equal-refutes direction ("if its work keeps standing, we're wrong" ↔ "an equal that relies on generated mortar genuinely refutes us") | same direction |

Register checks: metaphor domain is masonry-only in both (no strays); actor taxonomy
holds in the plain lens (agent/AI/model per C6).

## Notes (accepted, not defects)

1. Formal Claim 5 says "LLM" where the plain taxonomy would say "model" — carried
   verbatim from the `c252640` extension baseline; extension unaffected, and the term
   is precise for the formal register's declared attacker audience. Held as-is.
2. Plain Claim 1 carries the "not with a smarter model" emphasis absent from the
   formal statement; both knives share one refutation-set (a surviving autonomous
   system refutes either phrasing), so the emphasis is register, not extension.

**Verdict: coherent.** No changes required; the siblings agree in claim set,
extensions, honesty fields, pointers, and metaphor domain.
