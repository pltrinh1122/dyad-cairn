# Root Cause Analysis: node_4c_execute_anchor_compiler

## 1. Description of Execution
Implemented `skills/anchor_compiler.py` to deterministically project the `dip_state.yml` YAML structural source of truth into the physical `GEMINI.md` markdown anchor. 
This projection acts as a Materialized View for the Gemini LLM runtime.

## 2. Integration Details
- Hooked `anchor_compiler.compile_anchor()` directly into `skills/frontier_editor.py::save_state()`.
- Every time the machine state advances, the Anchor Compiler runs.
- If `dip_state.yml` is missing or incomplete, it falls back to generating the default Universal Shim, ensuring no crashes occur during early instantiation phases.

## 3. Invariant Falsification Validation
The hypothesis was that manual edits to `GEMINI.md` would lead to split-brain. 
By generating `GEMINI.md` dynamically on every state transition and placing a structural warning (`DO NOT EDIT THIS FILE DIRECTLY`), we have sealed the gap. `dip_state.yml` is now the sole mechanical authority over the 7 Dyad dimensions.
