node_todo_1781761164:
  title: 'Convert Todo: todo_1781761164'
  goal: 'Refactor the commissioned invariant extraction engine out of the native skills/
    directory into an isolated commissions/ boundary.


    WHAT: Move `skills/invariant_extractor.py` to `commissions/invariant_extractor.py`
    and move `tests/test_invariant_extractor.py` to `commissions/tests/test_invariant_extractor.py`.
    Update any Python module import paths so that `./bin/run-tests` can still successfully
    run the refactored test suite.

    WHY: To enforce the architectural Boundary Lock principle. We must strictly isolate
    commissioned third-party deliverables so they do not artificially conflate with
    our own local, load-bearing execution Substrate.'
  type: PLAN
  scope: SUBSTRATE
  status: AUTHORIZED
