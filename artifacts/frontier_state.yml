nodes:
  node_4b_execute_sonar_halting:
    dependencies: []
    status: READY
    goal: Implement the markdown parser in dip_sonar.py to dynamically extract the
      required dimensions, passing the TDD tests.
    title: Execute Provenance Extraction Invariant
    type: execute
  node_4c_probe_anchor_compilation:
    dependencies:
    - node_4a_execute_matrix_schema
    status: READY
    goal: Investigate how a fully saturated dip_state.yml is safely and deterministically
      projected into the immutable GEMINI.md anchor.
    title: Probe Anchor Compilation Invariant
    type: probe
  node_14_probe_state_sync_collision:
    dependencies: []
    status: READY
    goal: Investigate the Ledger and State Sync Collision. Determine the physical
      invariants for how concurrent agents mutating the same structural files (DYAD_LEDGER.md,
      frontier_state.yml) resolve data consistency and merge conflicts without shattering
      autonomous execution.
    title: Probe Ledger and State Sync Collision
    type: probe
