nodes:
  node_4b_execute_sonar_halting:
    dependencies: []
    status: READY
    goal: Implement the markdown parser in dip_sonar.py to dynamically extract the
      required dimensions, passing the TDD tests.
    title: Execute Provenance Extraction Invariant
    type: execute
  node_4c_probe_anchor_compilation:
    dependencies:
    - node_4a_execute_matrix_schema
    status: READY
    goal: Investigate how a fully saturated dip_state.yml is safely and deterministically
      projected into the immutable GEMINI.md anchor.
    title: Probe Anchor Compilation Invariant
    type: probe
  node_18_probe_spaor_plan_violation:
    dependencies:
    - node_17b_execute_audit_dag
    status: BLOCKED
    goal: (AUDIT DEBT) Investigate and remediate the systemic SPAOR violation where
      the Agent pre-emptively hallucinates [PLAN] nodes for unexecuted [PROBE] nodes.
      Implement the necessary CSI Guard or architectural constraints to trap this
      hallucination.
    title: Audit SPAOR PLAN Violation
    type: probe
  node_17b_execute_audit_dag:
    status: IN_REVIEW
    title: Execute Audit DAG Implementation
    goal: Parameterize the SPAOR engine and inject check_audit_lock() per plan_node_17a.md
    type: PLAN
