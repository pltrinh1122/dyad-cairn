nodes:
  node_4b_execute_sonar_halting:
    dependencies: []
    status: READY
    goal: Implement the markdown parser in dip_sonar.py to dynamically extract the
      required dimensions, passing the TDD tests.
    title: Execute Provenance Extraction Invariant
    type: execute
  node_4c_probe_anchor_compilation:
    dependencies:
    - node_4a_execute_matrix_schema
    status: READY
    goal: Investigate how a fully saturated dip_state.yml is safely and deterministically
      projected into the immutable GEMINI.md anchor.
    title: Probe Anchor Compilation Invariant
    type: probe
  node_12a_plan_worktree_substrate:
    dependencies: []
    status: BLOCKED
    goal: Design and formalize the Git Worktree partitioning strategy to safely isolate
      the Claude execution thread from the Architect's workspace based on the Context
      Partition invariant.
    title: Plan Local Worktree Partition
    type: plan
  node_12b_plan_authorization_handshake:
    dependencies:
    - node_12a_plan_worktree_substrate
    status: BLOCKED
    goal: Formalize the ./bin/claude-watch listener script to strictly halt Claude's
      execution pending the lean! authorization gate.
    title: Plan Executioner Authorization Handshake
    type: plan
  node_12c_plan_reflect_handoff:
    dependencies:
    - node_12a_plan_worktree_substrate
    status: BLOCKED
    goal: Formalize the mechanical transfer of the functional execution logs from
      Claude's Worktree back to the Architect to satisfy the structural REFLECT boundary.
    title: Plan REFLECT Execution Handoff
    type: plan
