nodes:
  node_4b_execute_sonar_halting:
    dependencies: []
    status: READY
    goal: Implement the markdown parser in dip_sonar.py to dynamically extract the
      required dimensions, passing the TDD tests.
    title: Execute Provenance Extraction Invariant
    type: execute
  node_4c_probe_anchor_compilation:
    dependencies:
    - node_4a_execute_matrix_schema
    status: READY
    goal: Investigate how a fully saturated dip_state.yml is safely and deterministically
      projected into the immutable GEMINI.md anchor.
    title: Probe Anchor Compilation Invariant
    type: probe
  node_12_plan_local_executioner_loop:
    dependencies: []
    status: READY
    goal: Design and formalize the Git Worktree partitioning strategy and the `./bin/claude-watch`
      listener script to safely isolate the Claude execution thread from the Architect's
      workspace without substrate entanglement.
    title: Plan Local Executioner Worktree Architecture
    type: plan
  node_13_probe_authorization_gate_collision:
    dependencies: []
    status: READY
    goal: Investigate the Authorization Gate Collision where a full-auto Executioner
      clone running PLAN+EXECUTE autonomously bypasses the HTIL Design-Review Gate
      ('lean!'). Determine the architectural invariant for mapping the SPAOR phases
      across the dual-agent boundary.
    title: Probe Authorization Gate Collision
    type: probe
