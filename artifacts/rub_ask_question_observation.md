# Observation Period: Rub Phase UI Lock

**Goal:** Establish an ongoing observation period where the Operator evaluates the Agent's inferential hit-rate during the Rub phase.

If the escape hatch is used too frequently, the interaction model will be flagged for re-evaluation. The `ask_question` lock provides a rigid modal that must remain a high-leverage feature (via quick exits) rather than annoying toil. We will rigorously calibrate the Agent's inferential accuracy.
