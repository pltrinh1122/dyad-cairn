# Reflect: Session Reflect - FSM Integrity & Commission Boundaries

## Continue
- Structurally verifying all code with TDD (the `bin/run-tests` non-negotiable) before invoking FSM node closure.
- Treating cross-dyad DMs as rigid mechanical contracts, explicitly mapping their acceptance criteria (e.g., `F-1...F-8`) to literal tests in the falsifier suite.
- Resolving mechanical DAG drifts autonomously via deterministic refactor scripts rather than hallucinated text.

## Start
- Strictly enforcing the Boundary Lock invariant by placing third-party commissioned deliverables (like the invariant extraction engine) into isolated bounds (e.g., `commissions/`) rather than polluting our own local, load-bearing execution Substrate (`skills/`).
- Triaging inbound `.md` messages from `dyad-state/inbox/` into formalized FSM `todo` intents at the very start of the session to prevent orphaned frictions.

## Stop
- Conflating local repository structure with commissioned products.
- Allowing Git working-tree ghost artifacts (e.g., `node_99_test.yml`) to resurrect dead nodes; always run a deterministic cleanup after branching back in time.
