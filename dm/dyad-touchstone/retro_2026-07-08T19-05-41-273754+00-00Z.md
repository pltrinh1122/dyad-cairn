# Reflect: d-land Port and Craft Refresh

## Continue
We will continue rigorously upholding the Mechanical Gate (CI) invariant prior to merging PRs. The physical presence of CSI Guards—such as the Commons Drift detector and the Topology Mass limits—proves invaluable for identifying structural violations before they pollute the `main` branch. 

## Start
We must start ensuring that whenever test suites assert behavior on physical files or directories (like `fsm_state.yml` or `artifacts/todos`), those tests are either fully isolated or cleaned up correctly to prevent spurious local test failures due to untracked artifacts.

## Stop
We must stop relying on implicit FSM state transitions (e.g. `arc-land`) that were previously hardcoded in Python wrappers now that we have ported the raw bash spines from the upstream commons. The topology must accurately reflect the executable reality; deleting the obsolete `tests/test_d_land_cli.py` correctly closed this gap.

## SH
Should Have: When the CI initially hung, the Operator stated: "github is a separate and asynchronous system so you can't assert it's state immediately." This was an assumption that the CI was failing simply due to standard network latency/asynchrony, when in reality the local CI script was physically hanging due to an unexpected `gh` interactive authentication prompt in the test suite execution. We should have isolated the failure mode at the test harness level rather than exclusively attributing it to remote pipeline latency.
