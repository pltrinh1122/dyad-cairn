# Retro: Historical Alignment & The Quarry Parser

**CONTINUE:**
- Strict `WIP-N=1` seriality on the Substrate to prevent split-brain; `WIP-N > 1` reserved for Payload parallelization.
- The Crucible of Falsification: Use the Retro phase to violently stress-test Operator feedback. Capture both survivors and mapped anti-patterns in the CSS matrix.
- Direct Cross-Dyad Comms: Route inter-dyad syncs via `dm/` outboxes without blocking for Operator PR approvals.
- Render DAGs explicitly as horizontal ASCII directory trees to respect terminal scaling.

**START:**
- **Deterministic Quarry Parsing:** Stop parsing the platform `brain/` multi-tenant cache with raw bash pipelines or active memory. Use the deterministic `skills/quarry_parser.py` tool. It enforces identity boundaries by verifying the Local Root Substrate signature (e.g., `dyad-cairn`) before extracting logs, mathematically preventing the assimilation of peer dyad memories (like `dyad-wu-wei`).
- **Traceability:** Explicitly trace the cognitive mapping from the raw friction seed to the final CSS rule.
- **Causal Clipping:** When writing to the Ledger, append the causal rationale ("WHY") alongside the mechanical rule to anchor future resolutions.
- **Closed-Ended HITL:** Provide actionable, multiple-choice selections at OBSERVE gates to reduce Operator cognitive load.
- **Bidirectional Retros:** Counter every Operator Retro with an internal Agent Retro to expose mechanical realities.

**STOP:**
- **Conversational Engine Bypasses:** Stop treating conversational closure ("stand down") as an excuse to bypass physical state-machine transitions (like executing dialect scripts or popping the LIFO stack).
- **Verbosity in OBSERVE:** Eliminate philosophical filler text during execution phases. Keep OBSERVE outputs strictly state-driven.
- **Premature PR Assertions:** Stop hallucinating PR states based on intent. Never present a PR to the HITL gate until `bin/run-tests` passes mechanically.
- **Relying on Ephemeral UUIDs:** The active LLM context and platform Conversation IDs are illusions. Truth exists exclusively in the deterministic output of the Quarry Parser and the local `DYAD_LEDGER.md`.
